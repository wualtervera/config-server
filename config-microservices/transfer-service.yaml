server:
  port: 8006
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka  #Referencia hacia eureka-service
    instance:
      hostname: localhost